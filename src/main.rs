use pfp::PFP;
use std::env;

fn main() {
    let args: Vec<String> = env::args().collect();
    //let config = PFP<u32, u32>::Config::new(5, 5).unwrap_or_else(|err| {
    //    println!("Problem parsing arguments: {}", err);
    //    process::exit(1);
    //});
}
